name: Publish Release to Another Repo

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cross-release:
    runs-on: ubuntu-latest
    if: ${{ github.ref == 'refs/heads/main' }}

    steps:
      - name: Checkout current repo
        uses: actions/checkout@v5

      - name: Checkout xcss-core (read-only)
        uses: actions/checkout@v5
        with:
          repository: yshell-dev/xcss-core
          token: ${{ secrets.CROSS_REPO_TOKEN }}
          path: core_repo

      - name: Get version from xcss-core & publish package to npm
        id: core-setup
        continue-on-error: true
        run: |
          cd core_repo
          echo "version=v$(jq -r .version package.json)" >> $GITHUB_OUTPUT
          npm ci
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.XCSS_NPM_PAT }}


      - name: Switch back to current repository
        run: cd "${GITHUB_WORKSPACE}"

      - name: Run build script
        run: |
          chmod +x ./scripts/build.sh
          ./scripts/build.sh

      - name: Prepare artifacts
        run: |
          mkdir -p artifacts
          cp -r ../bin/* artifacts/

      - name: Publish release back to xcss-core
        uses: softprops/action-gh-release@v2
        with:
          repository: yshell-dev/xcss-core
          token: ${{ secrets.CROSS_REPO_TOKEN }}
          tag_name: ${{ steps.corever.outputs.version }}
          name: Release ${{ steps.corever.outputs.version }}
          body_path: ./release.md
          files: artifacts/**
        env:
          GITHUB_TOKEN: ${{ secrets.CROSS_REPO_TOKEN }}
